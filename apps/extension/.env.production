# Production backend (Railway). Loaded only by `wxt build` (production mode);
# `wxt dev` keeps the localhost defaults. Untracked — safe to edit per environment.
WXT_PUBLIC_API_BASE=https://backend-production-dce82.up.railway.app
WXT_PUBLIC_SIGNAL_URL=wss://backend-production-dce82.up.railway.app/signal
